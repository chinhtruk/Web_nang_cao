<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quản Lý Sản Phẩm</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <nav class="navbar navbar-dark p-3" style="background-color: #ff4500;">
        <div class="container">
            <a class="navbar-brand text-white" href="/">🛍️ Shopee Clone</a>
            <a href="/admin/add" class="btn btn-light">➕ Thêm Sản Phẩm</a>
        </div>
    </nav>

    <div class="container mt-4">
        <h2 class="text-center text-danger">📋 Quản Lý Sản Phẩm</h2>

        <% if (products.length > 0) { %>
            <table class="table table-bordered mt-3">
                <thead class="table-warning">
                    <tr>
                        <th>Hình Ảnh</th>
                        <th>Tên Sản Phẩm</th>
                        <th>Giá</th>
                        <th>Mô Tả</th>
                        <th>Hành Động</th>
                    </tr>
                </thead>
                <tbody>
                    <% products.forEach(product => { %>
                        <tr>
                            <td><img src="<%= product.image %>" width="60" height="60" class="rounded border"></td>
                            <td><%= product.name %></td>
                            <td class="text-danger fw-bold">
                                <%= new Intl.NumberFormat('vi-VN', { style: 'currency', currency: 'VND' }).format(product.price) %>
                            </td>
                            <td><%= product.description %></td>
                            <td>
                                <a href="/admin/edit/<%= product._id %>" class="btn btn-info btn-sm">✏️ Sửa</a>
                                <form action="/admin/delete/<%= product._id %>" method="post" class="d-inline">
                                    <button type="submit" class="btn btn-danger btn-sm">🗑️ Xóa</button>
                                </form>
                            </td>
                        </tr>
                    <% }) %>
                </tbody>
            </table>
        <% } else { %>
            <p class="text-center mt-4 text-muted">Không có sản phẩm nào để hiển thị. 🛍️</p>
        <% } %>
    </div>

</body>
</html>