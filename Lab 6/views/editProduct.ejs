<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chỉnh Sửa Sản Phẩm</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

    <nav class="navbar navbar-dark p-3" style="background-color: #ff4500;">
        <div class="container">
            <a class="navbar-brand text-white" href="/admin">🔙 Quay lại Quản Lý</a>
        </div>
    </nav>

    <div class="container mt-5">
        <h2 class="text-center text-danger fw-bold">✏️ Chỉnh Sửa Sản Phẩm</h2>
        <div class="card shadow-lg p-4">
            <form action="/admin/edit/<%= product._id %>" method="POST" enctype="multipart/form-data">
                <div class="mb-3">
                    <label class="form-label fw-bold">Tên sản phẩm</label>
                    <input type="text" name="name" class="form-control" value="<%= product.name %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label fw-bold">Giá</label>
                    <input type="number" name="price" class="form-control" value="<%= product.price %>" required>
                </div>
                <div class="mb-3">
                    <label class="form-label fw-bold">Mô tả</label>
                    <textarea name="description" class="form-control" required><%= product.description %></textarea>
                </div>
                <div class="mb-3">
                    <label class="form-label fw-bold">Hình ảnh hiện tại</label>
                    <br>
                    <img src="<%= product.image %>" width="100" class="rounded border">
                </div>
                <div class="mb-3">
                    <label class="form-label fw-bold">Chọn ảnh mới (nếu có)</label>
                    <input type="file" name="image" class="form-control" accept="image/*">
                </div>
                <button type="submit" class="btn btn-success">💾 Lưu thay đổi</button>
            </form>
        </div>
    </div>

</body>
</html>